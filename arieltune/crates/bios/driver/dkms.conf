PACKAGE_NAME="smiflash"
PACKAGE_VERSION="0.1.0"
BUILT_MODULE_NAME[0]="smiflash"
DEST_MODULE_LOCATION[0]="/updates/dkms"
AUTOINSTALL="yes"
# Make an incomplete/stripped BC-250 headers tree buildable on the board first
# (no-op on a normal tree). See prepare.sh.
PRE_BUILD="prepare.sh ${kernelver}"
